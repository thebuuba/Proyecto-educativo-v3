{
  "name": "@aula/database",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate": "dotenv -e ../../apps/backend/.env -- prisma generate",
    "migrate": "dotenv -e ../../apps/backend/.env -- prisma migrate dev",
    "seed": "dotenv -e ../../apps/backend/.env -- tsx src/seed.ts",
    "studio": "dotenv -e ../../apps/backend/.env -- prisma studio",
    "build": "tsc"
  },
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^24.12.3",
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.5.0",
    "tsx": "^4.19.0",
    "typescript": "~6.0.2"
  }
}
